#add-portfolio-form{:style => 'display:none;'}
  = simple_form_for(Portfolio.new, :url => '#') do |f|
    = f.input :name
    = f.input :trust_level, :as => :select, :collection => trust_level_select, :label => 'Minimum TRUST level'
    = link_to 'Create', '#', :class => 'create_link'
