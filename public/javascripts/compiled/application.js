(function() {
  /*
  = require jquery
  = require jquery-ui
  = require autocomplete-rails
  = require jquery.facebox
  = require backbone
  = require ICanHaz
  = require date_formatter
  */  $(function() {
    return $('#main-logo').click(function() {
      return window.location = "/";
    });
  });
}).call(this);
